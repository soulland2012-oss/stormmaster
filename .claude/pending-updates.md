# Pending Updates

<!-- Сюда Claude записывает изменения, которые ещё не запушены.
     При команде "запушь в гит" — всё из этого файла идёт в коммит, затем файл очищается. -->

- Добавлена страница «Создать анкету» (`/characters/create`): форма из 10 пунктов анкеты персонажа (имя, параметры, расы-чипы, аура/магия с путями, статус, навыки, инвентарь, характер, биография, внешность), автосохранение черновика в localStorage, экспорт в PDF через `@react-pdf/renderer` (кириллические шрифты PT Serif + Cormorant Garamond, стилизовано под золото/пергамент сайта).
- Добавлен подпункт «Создать анкету» в навигации под «Персонажи».
- Добавлена CTA-ссылка на новую страницу в разделе «Введение» (2.3 Гайд по Анкете).
- Рефакторинг: вынесены общие данные о расах (`data/races.ts`) и пунктах анкеты (`data/anketa-fields.ts`) — использовались раньше как задублированные inline-массивы в `RacesHub.tsx` и `IntroBody.tsx`.
